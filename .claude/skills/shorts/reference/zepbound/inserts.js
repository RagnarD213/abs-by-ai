// No AI cover clips in the Zepbound batch. Every join here is hidden by the wide/tight punch
// alternation (plan.js). If Dan asks for a cover clip over a join, the mechanism is intact:
// { seg, afterPiece, clip, dur, pre, in } - see the supplements batch's inserts.js and aigen/.
module.exports = [];
