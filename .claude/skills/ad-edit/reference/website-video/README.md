# Moved (2026-09-09)

The website conversion video's recipe now lives with the skill that owns it:
`.claude/skills/website-video/reference/recipe/` (same files, same names — `layout.py`, `hairdet.py`, `hairgate.py`,
`tanpass.py`, `audio4.py`, `build_inserts.py`, `deliver.sh`, `rev6.sh` …). Lessons 73–122 in this SKILL.md still describe
that pipeline; read them with that path. One copy on purpose — two copies is how a pipeline was lost once (lesson under
"Animated graphics", 2026-08-24).
