# AV-02 — Ad 2 "Stop Wasting Money On Nutritionists": 9:16 ≤0:59 + 1:1 ≤0:59

**List 3 · ad variant · READY · replaces J3.** Read `00-RULES.md` first. Ad 2 took ~80% of the campaign's early spend.

## What exists
| variant | state |
|---|---|
| 16:9 (Muhammad) | filed, live `Dtk5knWM7c8`, 8,275 frames |
| 9:16 full | ✅ approved 09-08 (`7XgHxn59Tsg`), build `/Volumes/Extreme/_edit_work/ad2-vert-v2/` |
| 1:1 full | ✅ approved 09-12 (`hHiPzQKTzrg`), build `/Volumes/Extreme/_edit_work/ad2-sq/` |
| **9:16 59s + 1:1 59s** | ❌ **this job**. No `cut_plan.json` has ever existed for Ad 2. |

## Build
1. **Cut plan first**, on Muhammad's 8,275-frame timeline, using the `/shortad-from-longform` cutdown method: hook, problem,
   AI demo, payoff, CTA. Snap seams to silence and to HIS picture cuts, never inside a light leak (A8.13). **Exclude the
   email-capture screen** (his 16:9 shows it at 3:11, 3:12, 3:23). If a demo is kept, its before and after must be the same person.
2. **9:16 59s** from a copy of `ad2-vert-v2/`; **1:1 59s** from a copy of `ad2-sq/`, both off the same plan. Every real
   picture of Dan in range gets the real-picture chip, off face and abs. Match what the approved square already carries.
3. **Audio:** the approved vertical's AAC (the square carries the same stream), cut at the seams only. Its approved +5.2 dB
   lift is already in it, so add nothing.
4. Check the CTA-pill trailing-caption overprint on both. The approved Ad 2 vertical is known to carry it.
5. Render the two in sequence, not in parallel. If it needs two sessions, deliver the 9:16 first and update the master row.

## Deliver
`stop wasting money on nutritionists | claude | 9x16 59s | ad 2.mp4` and `… | 1x1 59s | ad 2.mp4` + review copies + stamps.

## Starter prompts
**Claude (Fable 5.1, high):**
> Read `Handoffs/video-editing/00-RULES.md`, then execute `Handoffs/video-editing/AV-02-ad2-cutdowns-vertical-and-square-59s.md`: build a ≤0:59 cut plan for Ad 2 on Muhammad's 8,275-frame timeline (no email-capture screen), then render the 9:16 59s from `ad2-vert-v2/` and the 1:1 59s from `ad2-sq/` off that one plan, with real-picture labels and the approved audio cut at the seams. Every gate, an independent audit on each, deliver, send me both review copies, update the master list.

**Codex (GPT-6 Astra, high):**
> Read `Handoffs/video-editing/00-RULES.md` (Codex column + environment table), then execute `Handoffs/video-editing/AV-02-ad2-cutdowns-vertical-and-square-59s.md` using `.claude/skills/shortad-from-longform/SKILL.md` as the method. Deliver, send Dan both review copies, update `00-MASTER.md`.
